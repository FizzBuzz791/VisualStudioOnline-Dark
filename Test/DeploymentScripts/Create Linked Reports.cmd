@echo off
:: ** Script generated by Reporting Services Scripter 2.0.0.17 **
:: ** Created by Jasper Smith (jas@sqldbatips.com)             **
:: ** See http://www.sqldbatips.com for help/support           **

::Script Variables
SET LOGFILE="RS Scripter Load Log.txt"
SET SCRIPTLOCATION=
SET BACKUPLOCATION=
SET REPORTSERVER=http://localhost:80/ReportServer
SET RS="C:\Program Files (x86)\Microsoft SQL Server\130\Tools\Binn\RS.EXE"
SET TIMEOUT=60

::Clear Log file
IF EXIST %logfile% DEL %logfile%

IF NOT EXIST %RS% (
	ECHO Unable to find RS.Exe at: %RS%
	EXIT /B -1
)



::Write Log Header
ECHO Reporting Services Scripter Load Log 2.0.0.17 >>%LOGFILE%
ECHO. >>%LOGFILE%
ECHO Starting Load at %DATE% %TIME% >>%LOGFILE%
ECHO SCRIPTLOCATION = %SCRIPTLOCATION% >>%LOGFILE%
ECHO REPORTSERVER   = %REPORTSERVER% >>%LOGFILE%
ECHO BACKUPLOCATION = %BACKUPLOCATION% >>%LOGFILE%
ECHO SCRIPTLEVEL    = SQL2008 >>%LOGFILE%
ECHO TIMEOUT        = %TIMEOUT% >>%LOGFILE%
ECHO RS             = %RS% >>%LOGFILE%
ECHO. >>%LOGFILE%

::Run Scripts

ECHO Running script "%SCRIPTLOCATION%BhpbioF1F2F3HUBGeometReconciliationReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioF1F2F3HUBGeometReconciliationReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1
ECHO. >>%LOGFILE%

ECHO Running script "%SCRIPTLOCATION%BhpbioF1F2F3ProductReconContributionReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioF1F2F3ProductReconContributionReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1

ECHO Running script "%SCRIPTLOCATION%BhpbioF1F2F3ReconciliationProductAttributeReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioF1F2F3ReconciliationProductAttributeReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1

ECHO Running script "%SCRIPTLOCATION%BhpbioProductSupplyChainMonitoringReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioProductSupplyChainMonitoringReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1

ECHO Running script "%SCRIPTLOCATION%BhpbioF1F2F3GeometOverviewReconContributionReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioF1F2F3GeometOverviewReconContributionReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1

ECHO Running script "%SCRIPTLOCATION%BhpbioF1F2F3GeometReconciliationAttributeReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioF1F2F3GeometReconciliationAttributeReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1

ECHO Running script "%SCRIPTLOCATION%BhpbioForwardErrorContributionContextReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioForwardErrorContributionContextReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1

ECHO Running script "%SCRIPTLOCATION%BhpbioMonthlySiteReconciliationReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioMonthlySiteReconciliationReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1

ECHO Running script "%SCRIPTLOCATION%BhpbioQuarterlySiteReconciliationReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioQuarterlySiteReconciliationReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1

ECHO Running script "%SCRIPTLOCATION%BhpbioQuarterlyHubReconciliationReport.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%BhpbioQuarterlyHubReconciliationReport.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1

ECHO. >>%LOGFILE%
ECHO Finished Load at %DATE% %TIME% >>%LOGFILE%
ECHO. >>%LOGFILE%
